{
  "name": "kanjigo",
  "version": "1.0.0",
  "private": true,
  "description": "Automated quality checks for the KanjiGO static game",
  "scripts": {
    "sync:data": "node scripts/sync-content-csv.js",
    "sync:questions": "node scripts/sync-question-workbook.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
